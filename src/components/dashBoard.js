import React from 'react';

function Dashboard(props) {
    return (
        <div>
            Name
            Name of the Novel
            Writer
            File
        </div>
    );
}

export default Dashboard;